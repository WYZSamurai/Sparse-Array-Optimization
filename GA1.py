import torch


# if torch.cuda.is_available():
#     device = torch.device("cuda")
# else:
#     device = torch.device("cpu")
device = torch.device("cpu")
cpu = torch.device("cpu")


def GA(dna: torch.Tensor):
    pass


if __name__ == "__name__":
    pass
